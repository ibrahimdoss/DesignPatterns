package computerSolution5;

public enum Notes {

	//TODO GEÇ YÜKLEME -- LAZY LOADING
	
//	Bina sürecinde nesnenin gerçekten oluşturulduğu an, en son build() çağrısı yapılıncaya kadar geciktirilebilir.
//			build() çağrısı yapılmadığında hiç bir nesne oluşturulmaz.
//			
//	Bu şekilde geç/sonradan yüklemeli (lazy-loaded) bina süreci oluşturulabilir.
//	
//	Bu amaçla builder nesnesi bina ettiği nesnenin durumuyla ilgili bilgi tutmalı ve build() 
//	çağrısında bu bilgilere göre nesneyi oluşturmalıdır.
	
	
	//TODO
	//Bu çözümde ComputerBuilder bina edilecek Computer’a eklenen nesnelerle ilgili durum bilgisi tutmakta
	//ve buna göre nesneyi en son build() çağrısıyla geç oluşturmaktadır.
	
//	ComputerBuilder, Computer’un varsayılan kurucusunu kullanmaktadır
	
	//build() metodunun içine dikkat edelim.
	
	// Computer’da set metotları yoktur ve tüm kurucuları dışarıya kapatılmıştır.
	
	//asagıdaki kod yukarıdaki commentte gerceklestirilen islemin koddaki örneğini simgeler.
	
//	public interface ComputerBuilderI {
//		…
//		public Computer build() {
//		computer = new Computer();
//		computer.name = name;
//		if (hasRam) {
//		RAM ram = new RAM();
//		computer.ram = ram;
//		}
//		if (hasCPU) {
//		CPU cpu = new CPU();
//		computer.cpu = cpu;
//		}
//		if(hasHD) {
//		HardDrive hd = new HardDrive();
//		computer.hd = hd;
//		}
//		…
//		return computer;
//		}
//		}
	
}
